1.
100 Trials

        params = {
            "num_iterations": trial.suggest_int("num_iterations", 6, 10),
            "num_epochs": trial.suggest_int("num_epochs", 1, 4),
            "batch_size": trial.suggest_categorical("batch_size", [8, 16, 24]),
            "body_lr": trial.suggest_float("body_learning_rate", 1e-6, 5e-5, log=True)
        }

Optimale Parameter:
'num_iterations': 8, 'num_epochs': 2, 'batch_size': 24, 'body_learning_rate': 1.015597754880736e-06

2. 1201194
100 Trials
        params = {
            "num_iterations": trial.suggest_int("num_iterations", 6, 10),
            "num_epochs": trial.suggest_int("num_epochs", 1, 3),
            "batch_size": trial.suggest_categorical("batch_size", [16, 24]),
            "body_lr": trial.suggest_float("body_learning_rate", 1e-6, 5e-5, log=True),
            "head_lr": trial.suggest_float("head_learning_rate", 1e-5, 1e-1, log=True)
        }


    Optimale Parameter:
        params={
        'num_iterations': 7,
        'num_epochs': 3,
        'batch_size': 16,
        'body_learning_rate': 2.803487859551249e-06,
         'head_learning_rate': 0.048379305984974585}

3. 1227932
100 Trials
        params = {
            "num_iterations": trial.suggest_int("num_iterations", 6, 9),
            "num_epochs": trial.suggest_int("num_epochs", 2, 4),
            "batch_size": trial.suggest_categorical("batch_size", [16]),
            "head_lr": trial.suggest_float("head_learning_rate", 1e-5, 1e-1, log=True)
        }

        Optimale parameter:

        params = {'num_iterations': 8,
         'num_epochs': 3,
         'batch_size': 16,
         'head_lr': 1.5339162591163613e-05}
